# Live Wedding Mode Specification

## Overview

"연정" 서비스의 예식 당일 라이브 모드 스펙입니다.
예식장에서 대형 화면에 표시하거나, 호스트가 실시간으로 관리하는 기능입니다.

---

## 핵심 기능

| 기능 | 설명 |
|------|------|
| **디지털 사이니지** | 예식장 대형 화면용 16:9 최적화 레이아웃 |
| **실시간 방명록** | 하객 축하 메시지 자동 롤링 |
| **갤러리 슬라이드쇼** | 웨딩 사진 자동 재생 |
| **QR 코드 표시** | 하객이 스캔하여 바로 접속 |
| **호스트 컨트롤** | 실시간 콘텐츠 관리 패널 |

---

## Routes

| 경로 | 용도 | 접근 |
|------|------|------|
| `/live/{slug}` | 디지털 사이니지 (공개) | 누구나 |
| `/live/{slug}/control` | 호스트 컨트롤 패널 | 인증 필요 |

---

## 1. 디지털 사이니지 모드

### Route
`/live/{slug}`

### 화면 비율
- 기본: 16:9 (1920x1080)
- 지원: 16:10, 4:3 자동 대응

### 레이아웃 옵션

#### Option A: 방명록 중심

```
┌────────────────────────────────────────────────────────────────────────────┐
│                                                                            │
│   ┌──────────────────────────┐    ┌──────────────────────────────────────┐ │
│   │                          │    │                                      │ │
│   │     [커플 사진]           │    │  💬 축하 메시지                       │ │
│   │                          │    │                                      │ │
│   │                          │    │  ┌──────────────────────────────────┐│ │
│   │                          │    │  │ 김철수                           ││ │
│   │                          │    │  │ "결혼 축하해! 행복하게 살아~"     ││ │
│   │   민영 ♥ 지훈             │    │  └──────────────────────────────────┘│ │
│   │   2025. 05. 16           │    │                                      │ │
│   │                          │    │  ┌──────────────────────────────────┐│ │
│   │                          │    │  │ 박영희                           ││ │
│   │   ┌────────────┐         │    │  │ "두 분의 앞날을 축복합니다 🎉"    ││ │
│   │   │  [QR Code] │         │    │  └──────────────────────────────────┘│ │
│   │   │            │         │    │                                      │ │
│   │   └────────────┘         │    │  ┌──────────────────────────────────┐│ │
│   │   청첩장 보기 ↗           │    │  │ 이민수                           ││ │
│   │                          │    │  │ "행복한 가정 이루시길~"           ││ │
│   └──────────────────────────┘    │  └──────────────────────────────────┘│ │
│                                    │                                      │ │
│                                    └──────────────────────────────────────┘ │
│                                                                            │
│   연정.                                                 더채플앳청담 3층    │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

#### Option B: 갤러리 중심

```
┌────────────────────────────────────────────────────────────────────────────┐
│                                                                            │
│                      민영 ♥ 지훈  ·  2025. 05. 16                          │
│                                                                            │
│   ┌──────────────────────────────────────────────────────────────────────┐ │
│   │                                                                      │ │
│   │                                                                      │ │
│   │                        [갤러리 슬라이드쇼]                            │ │
│   │                           (전체 화면)                                 │ │
│   │                                                                      │ │
│   │                                                                      │ │
│   │                                                                      │ │
│   └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│   ○ ○ ● ○ ○ ○                                              3 / 12         │
│                                                                            │
│   ┌────────────┐                                                          │
│   │  [QR Code] │  청첩장 보기                                              │
│   └────────────┘                                                          │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

#### Option C: 혼합 모드

```
┌────────────────────────────────────────────────────────────────────────────┐
│                                                                            │
│   ┌────────────────────────────────────┐  ┌──────────────────────────────┐ │
│   │                                    │  │                              │ │
│   │       [갤러리 슬라이드쇼]           │  │  💬 최근 메시지               │ │
│   │                                    │  │                              │ │
│   │                                    │  │  ┌──────────────────────────┐│ │
│   │                                    │  │  │ "축하해요~! 🎉"          ││ │
│   │                                    │  │  │ - 김철수                 ││ │
│   │                                    │  │  └──────────────────────────┘│ │
│   │                                    │  │                              │ │
│   └────────────────────────────────────┘  │  ┌──────────────────────────┐│ │
│                                           │  │ "행복하세요! 💕"         ││ │
│   민영 ♥ 지훈                             │  │ - 박영희                 ││ │
│   2025. 05. 16 · 더채플앳청담             │  └──────────────────────────┘│ │
│                                           │                              │ │
│   ┌────────────┐                          │  [QR]  축하 메시지 남기기     │ │
│   │  [QR Code] │ 청첩장 보기              │                              │ │
│   └────────────┘                          └──────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 실시간 방명록 롤링

### 동작 방식

```typescript
// Supabase Realtime 구독
const subscription = supabase
  .channel('guestbook')
  .on(
    'postgres_changes',
    {
      event: 'INSERT',
      schema: 'public',
      table: 'guestbook_entries',
      filter: `invitation_id=eq.${invitationId}`,
    },
    (payload) => {
      addNewMessage(payload.new);
    }
  )
  .subscribe();
```

### 메시지 표시 규칙

| 설정 | 값 |
|------|-----|
| 화면당 메시지 수 | 3~5개 |
| 자동 롤링 간격 | 5초 |
| 새 메시지 등장 | 페이드 인 + 슬라이드 업 |
| 비밀글 | 표시 안 함 |
| 숨김 처리된 글 | 표시 안 함 |

### 메시지 카드 UI

```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  김철수                                        방금 전        │
│                                                              │
│  "지훈아 민영아 결혼 진심으로 축하해! 🎉                      │
│   항상 행복하고 서로 아끼면서 살아~                           │
│   오늘 정말 예쁘다!"                                         │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 애니메이션

```typescript
// Framer Motion variants
const messageVariants = {
  initial: { opacity: 0, y: 50 },
  animate: { opacity: 1, y: 0 },
  exit: { opacity: 0, y: -50 },
};

// 자동 롤링
useEffect(() => {
  const interval = setInterval(() => {
    setVisibleMessages((prev) => {
      const next = [...prev.slice(1), messages[nextIndex]];
      return next;
    });
  }, 5000);
  
  return () => clearInterval(interval);
}, [messages]);
```

---

## 3. 갤러리 슬라이드쇼

### 동작 설정

| 설정 | 기본값 | 범위 |
|------|--------|------|
| 전환 간격 | 5초 | 3~10초 |
| 전환 효과 | Fade | Fade / Slide / Zoom |
| 순서 | 순차 | 순차 / 랜덤 |
| 반복 | ON | ON / OFF |

### 전환 효과

```typescript
const transitions = {
  fade: {
    initial: { opacity: 0 },
    animate: { opacity: 1 },
    exit: { opacity: 0 },
    transition: { duration: 1 },
  },
  slide: {
    initial: { x: '100%' },
    animate: { x: 0 },
    exit: { x: '-100%' },
    transition: { duration: 0.8 },
  },
  zoom: {
    initial: { scale: 1.1, opacity: 0 },
    animate: { scale: 1, opacity: 1 },
    exit: { scale: 0.9, opacity: 0 },
    transition: { duration: 1.2 },
  },
};
```

### Ken Burns 효과 (선택)

```css
@keyframes kenburns {
  0% { transform: scale(1) translate(0, 0); }
  100% { transform: scale(1.1) translate(-2%, -2%); }
}

.slideshow-image {
  animation: kenburns 10s ease-in-out infinite alternate;
}
```

---

## 4. QR 코드 표시

### QR 코드 용도

| QR | 연결 대상 | 설명 |
|-----|----------|------|
| 청첩장 | `/{slug}` | 메인 청첩장 페이지 |
| 방명록 | `/{slug}#guestbook` | 방명록 섹션으로 직접 이동 |
| RSVP | `/{slug}#rsvp` | 참석 여부 폼 |

### 동적 QR 전환

```typescript
const qrModes = [
  { label: '청첩장 보기', path: '' },
  { label: '축하 메시지 남기기', path: '#guestbook' },
  { label: '참석 여부 알리기', path: '#rsvp' },
];

// 10초마다 QR 모드 전환
useEffect(() => {
  const interval = setInterval(() => {
    setQrMode((prev) => (prev + 1) % qrModes.length);
  }, 10000);
  
  return () => clearInterval(interval);
}, []);
```

### QR 코드 스타일

```typescript
// 브랜드 컬러 적용
<QRCodeSVG
  value={`https://yeonjeong.kr/${slug}${qrModes[qrMode].path}`}
  size={200}
  bgColor="#FFFFFF"
  fgColor="#5D4E4E"
  level="M"
  includeMargin={true}
/>
```

---

## 5. 호스트 컨트롤 패널

### Route
`/live/{slug}/control`

### 인증
- 청첩장 소유자만 접근 가능
- 모바일 최적화 (예식 중 스마트폰 조작)

### 컨트롤 패널 UI

```
┌─────────────────────────────┐
│ 라이브 모드 컨트롤    [종료] │
├─────────────────────────────┤
│                             │
│  현재 모드                   │
│  ┌─────────────────────────┐│
│  │ ● 방명록  ○ 갤러리  ○ 혼합││
│  └─────────────────────────┘│
│                             │
│  ─────────────────────────  │
│                             │
│  📺 화면 설정               │
│  ┌─────────────────────────┐│
│  │ 방명록 롤링 속도         ││
│  │ [느림 ──●── 빠름]       ││
│  └─────────────────────────┘│
│  ┌─────────────────────────┐│
│  │ 슬라이드 전환 간격        ││
│  │ [3초 ──●── 10초]        ││
│  └─────────────────────────┘│
│                             │
│  ─────────────────────────  │
│                             │
│  💬 방명록 관리             │
│  ┌─────────────────────────┐│
│  │ 📢 새 메시지 2개         ││
│  └─────────────────────────┘│
│                             │
│  최근 메시지                 │
│  ┌─────────────────────────┐│
│  │ 김철수                   ││
│  │ "축하해요~!"             ││
│  │ [승인 ✓] [숨김 ✕]       ││
│  └─────────────────────────┘│
│  ┌─────────────────────────┐│
│  │ 익명                     ││
│  │ "ㅋㅋㅋㅋ"               ││
│  │ [승인 ✓] [숨김 ✕]       ││
│  └─────────────────────────┘│
│                             │
│  ─────────────────────────  │
│                             │
│  🔗 화면 공유                │
│  ┌─────────────────────────┐│
│  │ [QR] [링크 복사]         ││
│  │ yeonjeong.kr/live/...   ││
│  └─────────────────────────┘│
│                             │
└─────────────────────────────┘
```

### 실시간 동기화

```typescript
// 컨트롤 패널 → 사이니지 설정 전파
async function updateLiveSettings(settings: LiveSettings) {
  await supabase
    .from('invitation_live_settings')
    .upsert({
      invitation_id: invitationId,
      mode: settings.mode,
      scroll_speed: settings.scrollSpeed,
      slide_interval: settings.slideInterval,
    });
}

// 사이니지에서 설정 변경 구독
const settingsSubscription = supabase
  .channel('live-settings')
  .on(
    'postgres_changes',
    {
      event: '*',
      schema: 'public',
      table: 'invitation_live_settings',
      filter: `invitation_id=eq.${invitationId}`,
    },
    (payload) => {
      applySettings(payload.new);
    }
  )
  .subscribe();
```

### 메시지 관리

| 액션 | 설명 |
|------|------|
| 승인 | 화면에 표시 (기본) |
| 숨김 | 화면에서 즉시 제거 |
| 하이라이트 | 상단 고정 (1분간) |

```typescript
async function hideMessage(messageId: string) {
  await supabase
    .from('guestbook_entries')
    .update({ is_hidden: true })
    .eq('id', messageId);
    
  // 실시간으로 사이니지에서 제거됨
}

async function highlightMessage(messageId: string) {
  await supabase
    .from('guestbook_entries')
    .update({ highlighted_until: new Date(Date.now() + 60000) })
    .eq('id', messageId);
}
```

---

## 6. 데이터 모델

### LiveSettings 테이블

```prisma
model InvitationLiveSettings {
  id            String   @id @default(cuid())
  invitationId  String   @unique
  
  // Mode
  mode          LiveMode @default(GUESTBOOK)
  
  // Guestbook Settings
  scrollSpeed   Int      @default(5)      // 1-10
  messagesPerScreen Int  @default(3)      // 3-5
  
  // Gallery Settings
  slideInterval Int      @default(5)      // seconds
  transition    String   @default("fade") // fade, slide, zoom
  shuffle       Boolean  @default(false)
  
  // Display
  showQr        Boolean  @default(true)
  showBranding  Boolean  @default(true)
  
  // Timestamps
  updatedAt     DateTime @updatedAt
  
  // Relations
  invitation    Invitation @relation(fields: [invitationId], references: [id], onDelete: Cascade)
}

enum LiveMode {
  GUESTBOOK
  GALLERY
  MIXED
}
```

### GuestbookEntry 확장

```prisma
model GuestbookEntry {
  // ... 기존 필드
  
  // Live Mode
  isHidden        Boolean   @default(false)
  highlightedUntil DateTime?
}
```

---

## 7. 기술 구현

### Supabase Realtime 설정

```sql
-- Enable Realtime for tables
ALTER PUBLICATION supabase_realtime ADD TABLE guestbook_entries;
ALTER PUBLICATION supabase_realtime ADD TABLE invitation_live_settings;
```

### 전체 화면 모드

```typescript
function enterFullscreen() {
  const elem = document.documentElement;
  
  if (elem.requestFullscreen) {
    elem.requestFullscreen();
  } else if (elem.webkitRequestFullscreen) {
    elem.webkitRequestFullscreen();
  }
}

// 자동 전체 화면 (사용자 인터랙션 필요)
<button onClick={enterFullscreen}>
  🖥 전체 화면으로 보기
</button>
```

### 화면 꺼짐 방지

```typescript
// Wake Lock API
async function preventScreenSleep() {
  if ('wakeLock' in navigator) {
    try {
      const wakeLock = await navigator.wakeLock.request('screen');
      return wakeLock;
    } catch (err) {
      console.error('Wake Lock error:', err);
    }
  }
}
```

### 자동 재연결

```typescript
// 네트워크 끊김 시 자동 재연결
supabase.channel('guestbook')
  .on('system', { event: 'disconnect' }, () => {
    showReconnecting();
  })
  .on('system', { event: 'reconnect' }, () => {
    hideReconnecting();
    refetchMessages();
  })
  .subscribe();
```

---

## 8. UI/UX 가이드

### 폰트 크기 (대형 화면)

| 요소 | 크기 | 비고 |
|------|------|------|
| 커플 이름 | 72px | 메인 타이틀 |
| 날짜 | 36px | 서브 타이틀 |
| 메시지 작성자 | 28px | 방명록 |
| 메시지 내용 | 32px | 방명록 |
| QR 레이블 | 24px | 하단 |

### 애니메이션 타이밍

| 애니메이션 | 지속 시간 |
|-----------|----------|
| 메시지 등장 | 800ms |
| 메시지 퇴장 | 600ms |
| 슬라이드 전환 | 1000ms |
| 페이드 인/아웃 | 500ms |

### 색상 대비

- 밝은 배경에서도 가독성 확보
- 어두운 배경 옵션 제공 (저녁 예식)

---

## 9. 성능 최적화

### 이미지 프리로딩

```typescript
// 다음 슬라이드 미리 로드
useEffect(() => {
  const nextIndex = (currentIndex + 1) % images.length;
  const img = new Image();
  img.src = images[nextIndex].url;
}, [currentIndex, images]);
```

### 메모리 관리

```typescript
// 오래된 메시지 정리 (메모리 절약)
const MAX_MESSAGES_IN_MEMORY = 50;

function addNewMessage(message: GuestbookEntry) {
  setMessages((prev) => {
    const updated = [message, ...prev];
    if (updated.length > MAX_MESSAGES_IN_MEMORY) {
      return updated.slice(0, MAX_MESSAGES_IN_MEMORY);
    }
    return updated;
  });
}
```

### 연결 상태 표시

```
┌──────────────────────────────────────┐
│ ● 실시간 연결됨                       │  (정상)
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│ ○ 연결 중...                         │  (재연결)
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│ ⚠ 오프라인 - 새로고침 필요            │  (오류)
└──────────────────────────────────────┘
```

---

## 10. 예식장 설치 가이드

### 필요 장비

| 장비 | 권장 사양 |
|------|----------|
| 디스플레이 | 50인치 이상, 1080p |
| 재생 장치 | 노트북, 크롬캐스트, 또는 스마트 TV |
| 네트워크 | WiFi 또는 4G/5G |
| 브라우저 | Chrome 최신 버전 권장 |

### 설치 단계

1. 디스플레이에 재생 장치 연결
2. `yeonjeong.kr/live/{slug}` 접속
3. 전체 화면 모드 활성화
4. 호스트 컨트롤 패널로 설정 조정

### 트러블슈팅

| 문제 | 해결 |
|------|------|
| 화면 꺼짐 | 전원 설정에서 화면 끄기 비활성화 |
| WiFi 불안정 | 4G 핫스팟 사용 권장 |
| 메시지 안 나옴 | 새로고침 후 재연결 확인 |
| QR 스캔 안됨 | 화면 밝기 확인, QR 크기 조정 |
