# Horizontal Journey Theme Specification

## Overview

"연정" 서비스의 **가로 스크롤 여정 테마** 스펙입니다.
기존 세로 스크롤 청첩장과 차별화된, **3D 다이오라마 스타일의 가로 스크롤 경험**을 제공합니다.

### 컨셉

> **"두 사람의 이야기를 한 편의 여정으로"**

커플의 러브스토리를 가로로 펼쳐지는 풍경 속에서 따라가는 몰입형 경험.
골든 아워 실루엣 스타일의 프리미엄 비주얼이 3D 공간에 배치되어 깊이감을 제공합니다.

---

## 현재 구현 상태

### ✅ 완료 (Demo: `/demo/journey`)

| 기능 | 상태 | 설명 |
|------|------|------|
| 세로→가로 스크롤 변환 | ✅ | Lenis + Framer Motion |
| 3D 레이어 패럴랙스 | ✅ | 5개 레이어 (0.15x~0.55x 속도) |
| 골든 아워 스카이 | ✅ | Canvas 그라데이션 텍스처 |
| 산맥 실루엣 | ✅ | THREE.Shape 3레이어 |
| 나무 실루엣 | ✅ | 베지어 곡선 Shape |
| 벚꽃 나무 | ✅ | 유기적 클러스터 배치 |
| 커플 실루엣 | ✅ | 신랑/신부 Shape + 바운스 |
| 떠다니는 꽃잎 | ✅ | 40개 파티클 애니메이션 |
| 진행 표시 바 | ✅ | 5섹션 인디케이터 |
| 스크롤 힌트 | ✅ | 바운스 애니메이션 |
| 5개 컨텐츠 섹션 | ✅ | Intro/Story/Gallery/Wedding/End |

### 🔄 다음 단계

| 기능 | 우선순위 | 설명 |
|------|----------|------|
| 실제 사진 통합 | High | 갤러리 섹션에 실제 커플 사진 |
| RSVP/방명록 모달 | High | 버튼 클릭 시 모달 연동 |
| 모바일 터치 최적화 | Medium | 터치 제스처 개선 |
| 커스텀 일러스트 | Medium | 수채화/라인 스타일 옵션 |
| 에디터 통합 | Low | 테마 선택 UI |

---

## 핵심 특징

| 특징 | 설명 |
|------|------|
| **가로 스크롤** | 세로 스크롤 → 가로 카메라 이동으로 변환 |
| **3D 다이오라마** | 2D 실루엣이 3D 공간에 레이어로 배치 |
| **패럴랙스 효과** | 전경/중경/후경이 다른 속도로 이동 |
| **캐릭터 여정** | 커플 실루엣이 풍경을 따라 이동 |
| **환경 스토리텔링** | 섹션이 풍경 변화로 자연스럽게 전환 |

---

## 기술 스택

| 영역 | 기술 | 버전 | 용도 |
|------|------|------|------|
| 3D 렌더링 | React Three Fiber | ^9.5.0 | Three.js React 바인딩 |
| 3D 헬퍼 | @react-three/drei | ^10.7.7 | OrthographicCamera, Text |
| 스무스 스크롤 | Lenis | ^1.3.17 | 부드러운 스크롤 경험 |
| 스크롤 애니메이션 | Framer Motion | ^12.24.7 | useScroll, useTransform |
| 스타일링 | Tailwind CSS | ^4 | UI 컴포넌트 |

---

## 구현 아키텍처

### 스크롤 → 카메라 동기화

```typescript
import Lenis from 'lenis';
import { useScroll, useTransform, useSpring } from 'framer-motion';
import { useFrame } from '@react-three/fiber';

function useSmoothScroll() {
  useEffect(() => {
    const lenis = new Lenis({
      duration: 1.4,
      easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
      smoothWheel: true,
      wheelMultiplier: 0.8,
    });

    function raf(time: number) {
      lenis.raf(time);
      requestAnimationFrame(raf);
    }
    requestAnimationFrame(raf);

    return () => lenis.destroy();
  }, []);
}

function CameraController() {
  const { scrollYProgress } = useScroll();
  const smoothProgress = useSpring(scrollYProgress, { 
    stiffness: 100, 
    damping: 30 
  });
  const cameraX = useTransform(smoothProgress, [0, 1], [0, TOTAL_WIDTH]);

  useFrame(() => {
    camera.position.x = cameraX.get();
  });
}
```

### 3D 레이어 구조

```
Camera ← OrthographicCamera (zoom: 40)

z: -50  │ Sky Gradient (Canvas Texture)
z: -45  │ Stars (80 particles)
z: -35  │ Distant Mountains (parallax: 0.15x)
z: -25  │ Mid Mountains (parallax: 0.25x)
z: -18  │ Near Mountains (parallax: 0.35x)
z: -10  │ Tree Silhouettes (parallax: 0.5x)
z: -6~7 │ Cherry Blossom Trees (parallax: 0.55x)
z: -4   │ Floating Petals (animated)
z: -3   │ Ground + Path (textured)
z: 0    │ Content Sections (Intro, Story, Gallery, Wedding, End)
z: 3    │ Couple Silhouette (follows scroll)
```

### 패럴랙스 속도

| 레이어 | 속도 비율 | Z Position |
|--------|-----------|------------|
| 하늘/별 | 0x (고정) | -50, -45 |
| 먼 산 | 0.15x | -35 |
| 중간 산 | 0.25x | -25 |
| 가까운 산 | 0.35x | -18 |
| 나무 | 0.5x | -10 |
| 벚꽃나무 | 0.55x | -6~7 |
| 커플 | 1.0x | 3 |

---

## 비주얼 스타일

### 컬러 팔레트

```typescript
const COLORS = {
  // Sky (Golden Hour Gradient)
  skyTop: "#0f0c29",      // Deep purple-blue
  skyMid: "#302b63",      // Purple
  skyHorizon: "#e07b4f",  // Warm orange
  skyGlow: "#ffd194",     // Golden

  // Accent
  roseGold: "#b76e79",
  champagne: "#f7e7ce",
  accentGold: "#d4a574",

  // Nature Silhouettes
  mountainFar: "#4a3f6b",
  mountainMid: "#2d3a4f",
  mountainNear: "#1a2332",
  treeSilhouette: "#0d1117",

  // Cherry Blossom
  blossomPink: "#ffb5c2",
  blossomLight: "#ffd6e0",
  petalWhite: "#fff5f5",

  // UI
  textPrimary: "#2c2c2c",
  textSecondary: "#5a5a5a",
  cardBg: "#fffbf5",
};
```

### 디자인 원칙

| 원칙 | 설명 |
|------|------|
| **실루엣 기반** | 단순 기하학 아닌 자연스러운 베지어 곡선 |
| **대기 원근법** | 먼 레이어일수록 연하고 흐릿하게 |
| **골든 아워** | 따뜻하고 로맨틱한 황혼 색감 |
| **글래스모피즘** | 카드 UI에 투명도와 블러 효과 |

---

## 장면(Scene) 구성

### Scene 1: 인트로 (x: 8)

```
┌─────────────────────────────────────────┐
│     WEDDING INVITATION                  │
│                                         │
│        민영 ♥ 지훈                       │
│     ────────────────                    │
│     2025. 05. 16 Saturday               │
│     우리의 이야기가 시작됩니다             │
└─────────────────────────────────────────┘
```

### Scene 2: 우리의 이야기 (x: 30)

```
┌─────────────────────────────────────────┐
│          CHAPTER ONE                    │
│          How We Met                     │
│                                         │
│    ┌─────────────────────────────┐      │
│    │ 2020년 따뜻한 봄날,          │      │
│    │ 공통 친구의 소개로 처음...    │      │
│    └─────────────────────────────┘      │
└─────────────────────────────────────────┘
```

### Scene 3: 갤러리 (x: 55)

```
┌─────────────────────────────────────────┐
│          CHAPTER TWO                    │
│          Our Moments                    │
│                                         │
│  ┌───────┐  ┌───────┐  ┌───────┐       │
│  │ 2021  │  │ 2022  │  │ 2023  │       │
│  │ Spring│  │ Summer│  │ Winter│       │
│  └───────┘  └───────┘  └───────┘       │
└─────────────────────────────────────────┘
```

### Scene 4: 웨딩 정보 (x: 80)

```
┌─────────────────────────────────────────┐
│          THE BIG DAY                    │
│       Wedding Ceremony                  │
│                                         │
│  ┌─────────────┐  ┌─────────────┐       │
│  │ Date & Time │  │    Venue    │       │
│  │ 2025.05.16  │  │ 더채플앳청담 │       │
│  │ 토요일 오후2시│  │ 3층 그랜드홀│       │
│  └─────────────┘  └─────────────┘       │
└─────────────────────────────────────────┘
```

### Scene 5: 감사 인사 (x: 102)

```
┌─────────────────────────────────────────┐
│         WITH GRATITUDE                  │
│           Thank You                     │
│                                         │
│  ┌────────┐ ┌────────┐ ┌────────┐       │
│  │ 참석여부 │ │축하메시지│ │마음전하기│       │
│  └────────┘ └────────┘ └────────┘       │
│                                         │
│        Made with love by 연정           │
└─────────────────────────────────────────┘
```

---

## UI 컴포넌트

### 진행 표시 바

```typescript
function ProgressBar() {
  const sections = ["시작", "만남", "추억", "결혼식", "감사"];
  
  return (
    <div className="fixed bottom-6 left-1/2 -translate-x-1/2 z-50">
      <div style={{
        background: "rgba(255, 251, 245, 0.95)",
        backdropFilter: "blur(12px)",
        border: "1px solid rgba(212, 165, 116, 0.2)",
      }}>
        {sections.map((section, i) => (
          <div key={i}>
            <div style={{
              background: i <= currentIndex 
                ? `linear-gradient(135deg, ${COLORS.roseGold}, ${COLORS.accentGold})` 
                : "#e0d8d0",
            }} />
            <span>{section}</span>
          </div>
        ))}
      </div>
    </div>
  );
}
```

### 스크롤 힌트

```typescript
function ScrollHint() {
  return (
    <div className="fixed bottom-24 animate-bounce">
      <span>스크롤하여 시작하기</span>
      <ChevronDownIcon />
    </div>
  );
}
```

---

## 데이터 모델 확장

### Invitation 테이블 추가 필드

```prisma
model Invitation {
  // ... 기존 필드
  
  // Journey Theme
  isJourneyTheme    Boolean  @default(false)
  journeyStyle      String?  // "silhouette" | "watercolor" | "line" | "flat"
  characterType     String?  // "walking" | "bicycle" | "holding-hands"
  backgroundTheme   String?  // "golden-hour" | "spring" | "summer" | "winter"
  
  // Story Content
  stories           Json?
  // [
  //   { scene: "first-meet", title: "첫 만남", text: "...", date: "2020-03-15" },
  //   { scene: "proposal", title: "프로포즈", text: "...", date: "2024-12-24" },
  // ]
}
```

---

## 성능 최적화

### 현재 적용된 최적화

| 기술 | 설명 |
|------|------|
| `useMemo` | Shape, 텍스처, 데이터 배열 메모이제이션 |
| Canvas Texture | 런타임 그라데이션/텍스처 생성 |
| Frustum Culling | Three.js 기본 적용 |
| DPR 제한 | `dpr={[1, 2]}` 레티나 대응 |

### 추가 최적화 (TODO)

| 기술 | 우선순위 |
|------|----------|
| Instanced Mesh | 반복 요소 (별, 꽃잎) |
| Texture Atlas | 에셋 번들링 |
| LOD | 거리별 디테일 조절 |
| Lazy Loading | 장면별 지연 로드 |

---

## 모바일 대응

### 현재 상태

- 기본 터치 스크롤 동작
- 반응형 Progress Bar

### TODO

| 기능 | 설명 |
|------|------|
| 터치 제스처 | 스와이프로 섹션 이동 |
| 저사양 모드 | 레이어 수 감소 |
| 폴백 | WebGL 미지원 시 정적 이미지 |

---

## 에디터 통합 (TODO)

### 테마 선택 UI

```
┌─────────────────────────────────────────┐
│  테마 선택                              │
├─────────────────────────────────────────┤
│                                         │
│  📱 세로 스크롤 테마                     │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐           │
│  │로맨틱│ │클래식│ │모던 │ │가든 │           │
│  └────┘ └────┘ └────┘ └────┘           │
│                                         │
│  ─────────────────────────────────────  │
│                                         │
│  🎬 가로 여정 테마              ✨ NEW   │
│  ┌─────────────────────────────────┐   │
│  │ [가로 스크롤 미리보기 GIF]       │   │
│  │ "두 사람의 이야기를 한 편의 여정으로" │   │
│  └─────────────────────────────────┘   │
│                                         │
│  스타일: [실루엣 ▼]  배경: [골든아워 ▼]   │
│                                         │
└─────────────────────────────────────────┘
```

---

## 파일 구조

```
app/
├── demo/
│   └── journey/
│       └── page.tsx          # 현재 데모 페이지
│
└── [invitationSlug]/
    └── journey/
        └── page.tsx          # (TODO) 실제 청첩장 렌더링

components/
└── journey/                  # (TODO) 분리된 컴포넌트
    ├── Scene.tsx
    ├── layers/
    │   ├── Sky.tsx
    │   ├── Mountains.tsx
    │   ├── Trees.tsx
    │   └── Ground.tsx
    ├── sections/
    │   ├── IntroSection.tsx
    │   ├── StorySection.tsx
    │   ├── GallerySection.tsx
    │   ├── WeddingSection.tsx
    │   └── EndSection.tsx
    └── ui/
        ├── ProgressBar.tsx
        └── ScrollHint.tsx
```

---

## 구현 로드맵

### Phase 1: MVP ✅

- [x] 기본 가로 스크롤 구조
- [x] 정적 배경 레이어 (패럴랙스)
- [x] 5개 컨텐츠 섹션
- [x] 커플 실루엣 애니메이션
- [x] 진행 표시 UI

### Phase 2: 라이브러리 통합 🔄

- [ ] Lenis 스무스 스크롤 적용
- [ ] Framer Motion useScroll/useTransform 적용
- [ ] 스프링 기반 부드러운 카메라 이동

### Phase 3: 인터랙션

- [ ] 클릭 가능한 요소 (사진, 버튼)
- [ ] 모달 통합 (RSVP, 방명록, 계좌)
- [ ] 진행바 클릭으로 섹션 이동

### Phase 4: 풍부화

- [ ] 다양한 비주얼 스타일 옵션
- [ ] 에디터 통합
- [ ] 실제 사진/데이터 연동

### Phase 5: 최적화

- [ ] 모바일 터치 최적화
- [ ] 성능 최적화 (저사양 모드)
- [ ] 폴백 처리
- [ ] 로딩 전략

---

## 참고 자료

### 영감 (Inspiration)

- [Mr. Panda's Portfolio](https://www.mr-pandas-psychologically-safe-portfolio.com/) - 가로 스크롤 + 패럴랙스
- 팝업북/종이공예 스타일
- 골든 아워 실루엣 사진

### 사용 라이브러리

| 라이브러리 | 문서 |
|-----------|------|
| React Three Fiber | https://docs.pmnd.rs/react-three-fiber |
| @react-three/drei | https://github.com/pmndrs/drei |
| Lenis | https://github.com/darkroomengineering/lenis |
| Framer Motion | https://www.framer.com/motion/ |
