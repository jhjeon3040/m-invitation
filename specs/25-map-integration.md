# Map Integration Specification

## Overview

"연정" 서비스의 지도 연동 스펙입니다.
예식장 위치 표시, 길찾기, 주소 검색 기능을 포함합니다.

---

## Map Provider 선택

### 비교

| 항목 | 카카오맵 | 네이버맵 |
|------|---------|---------|
| 국내 POI 정확도 | ★★★★★ | ★★★★★ |
| 무료 할당량 | 300,000/일 | 200,000/일 |
| 카카오톡 연동 | ✅ 네이티브 | ❌ |
| 길찾기 앱 연동 | 카카오맵 | 네이버맵 |
| T맵 연동 | ❌ | ❌ |
| 커스텀 스타일 | 제한적 | 제한적 |

### 선택: 카카오맵 (Primary)

- 주 공유 채널이 카카오톡이므로 카카오맵 우선
- 카카오 네비게이션 딥링크 연동
- 네이버맵/T맵은 길찾기 딥링크로 지원

---

## API Keys

### 환경변수

```bash
# .env.local
NEXT_PUBLIC_KAKAO_MAP_KEY=       # JavaScript 앱 키
NEXT_PUBLIC_KAKAO_REST_KEY=      # REST API 키 (주소 검색용)
```

### 도메인 등록

카카오 개발자 콘솔에서 허용 도메인 등록:
- `localhost` (개발)
- `yeonjeong.kr` (프로덕션)
- `*.vercel.app` (미리보기)

---

## 주소 검색 (에디터)

### 카카오 주소 API

```typescript
// lib/kakao-address.ts
interface AddressResult {
  placeName: string;
  address: string;
  roadAddress: string;
  lat: number;
  lng: number;
  phone?: string;
}

export async function searchAddress(query: string): Promise<AddressResult[]> {
  const response = await fetch(
    `https://dapi.kakao.com/v2/local/search/keyword.json?query=${encodeURIComponent(query)}`,
    {
      headers: {
        Authorization: `KakaoAK ${process.env.NEXT_PUBLIC_KAKAO_REST_KEY}`,
      },
    }
  );
  
  const data = await response.json();
  
  return data.documents.map((doc: any) => ({
    placeName: doc.place_name,
    address: doc.address_name,
    roadAddress: doc.road_address_name,
    lat: parseFloat(doc.y),
    lng: parseFloat(doc.x),
    phone: doc.phone,
  }));
}
```

### 검색 UI

```
┌─────────────────────────────────────────────────────────────┐
│  예식장 검색                                                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  🔍 [더채플                                              ]  │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  📍 더채플앳청담                                             │
│     서울 강남구 선릉로 158길 11                              │
│     02-1234-5678                                            │
├─────────────────────────────────────────────────────────────┤
│  📍 더채플앳논현                                             │
│     서울 강남구 논현로 168길 31                              │
│     02-2345-6789                                            │
├─────────────────────────────────────────────────────────────┤
│  📍 더채플앳삼성                                             │
│     서울 강남구 삼성로 96길 23                               │
│     02-3456-7890                                            │
└─────────────────────────────────────────────────────────────┘
```

### 선택 후 UI

```
┌─────────────────────────────────────────────────────────────┐
│  ✓ 더채플앳청담                                    [변경]   │
│    서울 강남구 선릉로 158길 11                               │
├─────────────────────────────────────────────────────────────┤
│  층/홀 (선택)                                               │
│  [3층 그랜드홀                                          ]   │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │              [지도 미리보기]                         │   │
│  │                    📍                               │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  위치가 정확하지 않나요? [직접 위치 조정]                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 직접 위치 조정

마커 드래그로 정확한 위치 지정:

```typescript
// 마커 드래그 가능 설정
const marker = new kakao.maps.Marker({
  position: new kakao.maps.LatLng(lat, lng),
  draggable: true,
});

kakao.maps.event.addListener(marker, 'dragend', () => {
  const position = marker.getPosition();
  onLocationChange({
    lat: position.getLat(),
    lng: position.getLng(),
  });
});
```

---

## 지도 표시 (게스트 뷰)

### 카카오맵 SDK 로드

```typescript
// components/KakaoMap.tsx
'use client';

import { useEffect, useRef } from 'react';
import Script from 'next/script';

interface KakaoMapProps {
  lat: number;
  lng: number;
  venueName: string;
  venueAddress: string;
}

export function KakaoMap({ lat, lng, venueName, venueAddress }: KakaoMapProps) {
  const mapRef = useRef<HTMLDivElement>(null);
  const mapInstance = useRef<any>(null);

  useEffect(() => {
    if (!window.kakao?.maps || !mapRef.current) return;

    const position = new window.kakao.maps.LatLng(lat, lng);
    
    const map = new window.kakao.maps.Map(mapRef.current, {
      center: position,
      level: 3,
    });
    
    const marker = new window.kakao.maps.Marker({
      position,
      map,
    });
    
    const infoWindow = new window.kakao.maps.InfoWindow({
      content: `
        <div style="padding: 10px; min-width: 150px;">
          <strong>${venueName}</strong>
        </div>
      `,
    });
    
    infoWindow.open(map, marker);
    
    mapInstance.current = map;
  }, [lat, lng, venueName]);

  return (
    <>
      <Script
        src={`//dapi.kakao.com/v2/maps/sdk.js?appkey=${process.env.NEXT_PUBLIC_KAKAO_MAP_KEY}&autoload=false`}
        strategy="lazyOnload"
        onLoad={() => {
          window.kakao.maps.load(() => {
            // 지도 초기화
          });
        }}
      />
      <div ref={mapRef} className="w-full h-64 rounded-lg" />
    </>
  );
}
```

### 지도 UI (게스트 뷰)

```
┌─────────────────────────────────────────────────────────────┐
│  📍 오시는 길                                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │                    [카카오맵]                        │   │
│  │                       📍                            │   │
│  │                  더채플앳청담                         │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  더채플앳청담 3층 그랜드홀                                   │
│  서울 강남구 선릉로 158길 11                                │
│                                                             │
│  ┌─────────────┐ ┌─────────────┐                           │
│  │ 📋 주소 복사 │ │ 🗺 길찾기   │                           │
│  └─────────────┘ └─────────────┘                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 길찾기 딥링크

### 길찾기 버튼 UI

```
┌─────────────────────────────────────────────────────────────┐
│  길찾기                                                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐     │
│  │               │ │               │ │               │     │
│  │  [카카오맵]    │ │  [네이버맵]    │ │   [T맵]      │     │
│  │               │ │               │ │               │     │
│  └───────────────┘ └───────────────┘ └───────────────┘     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 딥링크 구현

```typescript
// lib/navigation.ts

interface NavigationParams {
  lat: number;
  lng: number;
  name: string;
}

export function openKakaoMap({ lat, lng, name }: NavigationParams) {
  const url = `kakaomap://look?p=${lat},${lng}`;
  const webUrl = `https://map.kakao.com/link/to/${encodeURIComponent(name)},${lat},${lng}`;
  
  openDeepLink(url, webUrl);
}

export function openNaverMap({ lat, lng, name }: NavigationParams) {
  const url = `nmap://route/public?dlat=${lat}&dlng=${lng}&dname=${encodeURIComponent(name)}&appname=yeonjeong`;
  const webUrl = `https://map.naver.com/v5/directions/-/-/-/transit?c=${lng},${lat},15,0,0,0,dh`;
  
  openDeepLink(url, webUrl);
}

export function openTMap({ lat, lng, name }: NavigationParams) {
  const url = `tmap://route?goalx=${lng}&goaly=${lat}&goalname=${encodeURIComponent(name)}`;
  const webUrl = `https://apis.openapi.sk.com/tmap/app/routes?goalx=${lng}&goaly=${lat}&goalname=${encodeURIComponent(name)}`;
  
  openDeepLink(url, webUrl);
}

function openDeepLink(appUrl: string, webUrl: string) {
  const isIOS = /iPhone|iPad|iPod/.test(navigator.userAgent);
  const isAndroid = /Android/.test(navigator.userAgent);
  
  if (isIOS || isAndroid) {
    window.location.href = appUrl;
    
    setTimeout(() => {
      window.location.href = webUrl;
    }, 1500);
  } else {
    window.open(webUrl, '_blank');
  }
}
```

### 카카오 인앱 브라우저 대응

```typescript
export function openKakaoMapInKakao({ lat, lng, name }: NavigationParams) {
  const isKakaoInApp = /KAKAOTALK/i.test(navigator.userAgent);
  
  if (isKakaoInApp) {
    window.location.href = `kakaomap://look?p=${lat},${lng}`;
  } else {
    openKakaoMap({ lat, lng, name });
  }
}
```

---

## 주소 복사

### 구현

```typescript
async function copyAddress(address: string, venueName: string) {
  const fullAddress = `${venueName}\n${address}`;
  
  try {
    await navigator.clipboard.writeText(fullAddress);
    toast.success('주소가 복사되었습니다');
  } catch {
    // Fallback
    const textarea = document.createElement('textarea');
    textarea.value = fullAddress;
    document.body.appendChild(textarea);
    textarea.select();
    document.execCommand('copy');
    document.body.removeChild(textarea);
    toast.success('주소가 복사되었습니다');
  }
}
```

---

## 교통 안내

### 에디터 입력

```
┌─────────────────────────────────────────────────────────────┐
│  교통 안내 (선택)                                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  🚇 지하철                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 선릉역 5번 출구에서 도보 5분                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  🚌 버스                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 강남역 정류장 하차 (140, 144번)                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  🚗 자가용                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 네비게이션 "더채플앳청담" 검색                        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  🅿️ 주차                                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 건물 내 주차 가능 (2시간 무료)                        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 게스트 뷰 (Accordion)

```
┌─────────────────────────────────────────────────────────────┐
│  🚇 지하철                                              ▼   │
├─────────────────────────────────────────────────────────────┤
│  선릉역 5번 출구에서 도보 5분                               │
│  - 2호선 선릉역 5번 출구                                    │
│  - 분당선 선릉역 5번 출구                                   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  🚌 버스                                                ▶   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  🚗 자가용 / 🅿️ 주차                                    ▶   │
└─────────────────────────────────────────────────────────────┘
```

---

## 데이터 모델

### Invitation 테이블 (관련 필드)

```prisma
model Invitation {
  // ... 기존 필드
  
  // Venue
  venueName     String?
  venueAddress  String?       // 도로명 주소
  venueFloor    String?       // "3층 그랜드홀"
  venueLat      Float?
  venueLng      Float?
  venuePhone    String?
  
  // Transportation (JSON)
  transportation Json?
  // {
  //   subway: "선릉역 5번 출구에서 도보 5분",
  //   bus: "강남역 정류장 하차",
  //   car: "네비게이션 '더채플앳청담' 검색",
  //   parking: "건물 내 주차 가능 (2시간 무료)"
  // }
}
```

---

## 성능 최적화

### 지도 Lazy Loading

```typescript
// 스크롤 시 지도 로드
const [isMapVisible, setIsMapVisible] = useState(false);

useEffect(() => {
  const observer = new IntersectionObserver(
    ([entry]) => {
      if (entry.isIntersecting) {
        setIsMapVisible(true);
        observer.disconnect();
      }
    },
    { rootMargin: '200px' }
  );
  
  if (mapContainerRef.current) {
    observer.observe(mapContainerRef.current);
  }
  
  return () => observer.disconnect();
}, []);

return (
  <div ref={mapContainerRef}>
    {isMapVisible ? (
      <KakaoMap lat={lat} lng={lng} venueName={venueName} />
    ) : (
      <div className="w-full h-64 bg-gray-100 animate-pulse rounded-lg" />
    )}
  </div>
);
```

### 정적 지도 이미지 (Fallback)

```typescript
// 지도 SDK 로드 실패 시 정적 이미지
function getStaticMapUrl(lat: number, lng: number): string {
  return `https://map.kakao.com/api/staticmap/marker/${lng},${lat}?level=3&w=400&h=300`;
}
```

---

## 에러 처리

| 에러 | 처리 |
|------|------|
| 주소 검색 실패 | "검색 결과가 없습니다. 다른 키워드로 검색해보세요" |
| 지도 로드 실패 | 정적 이미지 + 주소 텍스트 표시 |
| 좌표 없음 | "지도 정보가 없습니다" + 주소만 표시 |
| 길찾기 앱 미설치 | 웹 버전으로 자동 전환 |

---

## 접근성

### 스크린리더 대응

```tsx
<div
  role="application"
  aria-label={`${venueName} 지도`}
  aria-describedby="map-description"
>
  <div id="map-description" className="sr-only">
    {venueName}, {venueAddress}에 위치한 예식장입니다.
    아래 버튼으로 길찾기를 시작할 수 있습니다.
  </div>
  <KakaoMap {...props} />
</div>
```

### 키보드 네비게이션

```tsx
<button
  onClick={() => openKakaoMap(venue)}
  onKeyDown={(e) => {
    if (e.key === 'Enter' || e.key === ' ') {
      openKakaoMap(venue);
    }
  }}
  aria-label="카카오맵으로 길찾기"
>
  카카오맵
</button>
```
